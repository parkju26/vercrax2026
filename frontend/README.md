# Vercrax Frontend (Next.js App Router)

## Setup
```bash
cd frontend
npm i
npm run dev
```

## Backend 연결
기본값은 http://localhost:3000 을 사용합니다.

원하면 .env.local에:
```
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000
```
